LONG TODOS:
+ price or time or level to unlock towers
+ UI music
+ Teleports
+ Explosive enemies
+ Ability enemies (10s imunity - no demage (50% chance on spawn), Explosive enemies)
+ heroes like ablity - on the start you will choose hero whit special ablity
+ ability leveling
+ multiple block towers
+ boss summoning (Custom bosses send after click, whit some reload 3 per games)
+ Camping
  " + bulding time" , " + zvuky stavění"
+ New Ability - 'Lightining strikes, strikes directly enemies'
+ New Ability - 'Swamp'
  "TODO: Improve - Editor - Json - Colored json",
  "TODO: Improve - Editor - Json - Json checks more then format and paths",
  "???TODO: New Ability - 'Freeze'",
---------------------------------------------------------------------------------------------
Pre-beta V.:
  "TODO: Improve - PERFORMANCE BOOST"
    - Mostly not about content, purely engine perf. Target: 40 enemies + 20 towers + 2 concurrent Towers Fury, sustained 120+ FPS.
    - Tower targeting (Tower.js): sort in-range candidates by distance, run checkLineOfSight() only from nearest outward with early-exit (same "nearest with clear LOS" result, fewer LOS checks). Consider a spatial grid so a tower only scans nearby enemies instead of the whole list.
    - Bullet terrain check (Bullet.js): replace the chained === blocking-tile comparisons with a Set lookup; skip the check entirely while a bullet stays inside the same tile it was last checked in.
    - Towers Fury multiplier (Tower.js): compute once per frame (Game/AbilityManager), pass the result into each tower's update() instead of every tower re-scanning all abilities.
    - updateUI() batching (Game.js): call it once after the enemies.filter() pass instead of once per kill/escape in the same frame.
    - All of the above should be behavior-preserving (same target picked, same bullet collision moment, same buffs, same final UI values) - pure implementation speedups, not gameplay changes.
  "TODO: Improve - Gameplay - Improve arrow calculations to improve Performance",
  "TODO: Improve - Menu - New Game Selector ??",
  "TODO: Improve - Do something about LORE maybe first campaign?? [Pre-beta V ??]",
  "TODO: Improve - Hero Camping",

Beta 1.0